<header>
  <nav>
    <a href="/">Home</a>
    <% if (!user) { %>
      <a href="/login">Login</a>
      <a href="/register">Sign Up</a>
    <% } else { %>
      <p>WELCOME</p>
      <%= user.username %>
      <a href="/log-out">Logout</a>
    <% } %>
  </nav>
</header>